import { HabilitardocenteDirective } from './habilitardocente.directive';

describe('HabilitardocenteDirective', () => {
  it('should create an instance', () => {
    const directive = new HabilitardocenteDirective();
    expect(directive).toBeTruthy();
  });
});
