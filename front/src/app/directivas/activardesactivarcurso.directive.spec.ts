import { ActivardesactivarcursoDirective } from './activardesactivarcurso.directive';

describe('ActivardesactivarcursoDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivardesactivarcursoDirective();
    expect(directive).toBeTruthy();
  });
});
