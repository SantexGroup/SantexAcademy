import { HabilitarmatriculaDirective } from './habilitarmatricula.directive';

describe('HabilitarmatriculaDirective', () => {
  it('should create an instance', () => {
    const directive = new HabilitarmatriculaDirective();
    expect(directive).toBeTruthy();
  });
});
