import { EstadoCursoDirective } from './estado-curso.directive';

describe('EstadoCursoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoCursoDirective();
    expect(directive).toBeTruthy();
  });
});
