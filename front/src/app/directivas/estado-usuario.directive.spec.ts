import { EstadoUsuarioDirective } from './estado-usuario.directive';

describe('EstadoUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
